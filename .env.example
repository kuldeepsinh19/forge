# Required: your Anthropic API key.
ANTHROPIC_API_KEY=

# Optional: GitHub token used only when opening a pull request.
# Needs `repo` scope. Forge never writes this into a prompt or a sandbox.
GITHUB_TOKEN=
